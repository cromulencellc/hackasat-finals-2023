
this utility makes the hashed passwords used in the .access files the webserver uses to authenticate users in protected folders. The format of the .access file is:

<Realm Name>
<username>:<password hash>
<username2>:<password hash2>
